---
import LucideIcon from './LucideIcon';
import ContactForm from './ContactForm';
---

<section class="py-20 px-6 bg-[#1a1a1a]" id="contact">
  <div class="max-w-[1200px] mx-auto">
    <div class="text-center mb-14" data-animate>
      <p class="uppercase text-xs font-bold tracking-[3px] text-accent mb-3">
        Contact
      </p>
      <h2
        class="text-[1.85rem] md:text-[3rem] font-bold text-white mb-4 leading-tight tracking-tight font-serif"
      >
        Let's Talk Operations
      </h2>
      <div class="w-16 h-[3px] bg-accent mx-auto mb-4"></div>
    </div>

    <!-- Contact Form Card -->
    <div class="max-w-[700px] mx-auto mb-14">
      <div
        class="bg-card-bg rounded-2xl p-10 border border-card-border"
      >
        <ContactForm client:load />
      </div>
    </div>

    <!-- Contact Info Below -->
    <div class="max-w-[700px] mx-auto">
      <p class="text-white/60 mb-9 leading-relaxed text-center">
        Prefer to reach out directly? Contact us by phone or email and we will respond within one business day.
      </p>

      <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
        <div class="flex items-start gap-4">
          <div
            class="w-11 h-11 bg-accent/15 rounded-xl flex items-center justify-center shrink-0"
          >
            <LucideIcon name="Mail" size={20} className="text-accent" />
          </div>
          <div>
            <div
              class="text-xs text-white/40 uppercase tracking-[1px] font-semibold mb-0.5"
            >
              Email
            </div>
            <div class="text-[0.95rem] text-white font-medium">
              <a
                href="mailto:getintouch@TheERFG.com"
                class="hover:text-accent transition-colors"
              >
                getintouch@TheERFG.com
              </a>
            </div>
          </div>
        </div>

        <div class="flex items-start gap-4">
          <div
            class="w-11 h-11 bg-accent/15 rounded-xl flex items-center justify-center shrink-0"
          >
            <LucideIcon name="Phone" size={20} className="text-accent" />
          </div>
          <div>
            <div
              class="text-xs text-white/40 uppercase tracking-[1px] font-semibold mb-0.5"
            >
              Phone
            </div>
            <div class="text-[0.95rem] text-white font-medium">
              <a
                href="tel:6464506051"
                class="hover:text-accent transition-colors"
              >
                646-450-6051
              </a>
            </div>
          </div>
        </div>

        <div class="flex items-start gap-4">
          <div
            class="w-11 h-11 bg-accent/15 rounded-xl flex items-center justify-center shrink-0"
          >
            <LucideIcon name="MapPin" size={20} className="text-accent" />
          </div>
          <div>
            <div
              class="text-xs text-white/40 uppercase tracking-[1px] font-semibold mb-0.5"
            >
              Address
            </div>
            <div class="text-[0.95rem] text-white font-medium">
              456A Central Ave Suite 115, Cedarhurst, NY 11516
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
